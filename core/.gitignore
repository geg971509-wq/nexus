*.log
*.pem
# sing-box CLI default config path when Core is run standalone (boxmain/main.go).
# Narrow on purpose: a bare `*.json` silently swallows any testdata fixture.
config.json
*.exe
*.dat
*.db
server/gen/*.pb.go
server/gen/*.pb.*.go
# build.sh overlay modfile: replaces sing-box with the patched copy under bin/
server/go.patched.mod
server/go.patched.sum
